import "./chunk-7OAPEGJQ.mjs";
import JSON5 from "json5";
function extractJson(text) {
  let openingChar;
  let closingChar;
  let startPos;
  let nestingCount = 0;
  for (let i = 0; i < text.length; i++) {
    const char = text[i].replace(/\u00A0/g, " ");
    if (!openingChar && (char === "{" || char === "[")) {
      openingChar = char;
      closingChar = char === "{" ? "}" : "]";
      startPos = i;
      nestingCount++;
    } else if (char === openingChar) {
      nestingCount++;
    } else if (char === closingChar) {
      nestingCount--;
      if (!nestingCount) {
        return JSON5.parse(text.substring(startPos || 0, i + 1));
      }
    }
  }
  if (startPos !== void 0 && nestingCount > 0) {
    try {
      return JSON5.parse(text.substring(startPos) + (closingChar || ""));
    } catch (e) {
      throw new Error(`Invalid JSON extracted from model output: ${text}`);
    }
  }
  throw new Error(`No JSON object or array found in model output: ${text}`);
}
export {
  extractJson
};
//# sourceMappingURL=extract.mjs.map