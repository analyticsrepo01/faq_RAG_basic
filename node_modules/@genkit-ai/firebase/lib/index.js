"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};
var src_exports = {};
__export(src_exports, {
  defineFirestoreRetriever: () => import_firestoreRetriever.defineFirestoreRetriever,
  firebase: () => firebase
});
module.exports = __toCommonJS(src_exports);
var import_core = require("@genkit-ai/core");
var import_flow = require("@genkit-ai/flow");
var import_firestoreTraceStore = require("./firestoreTraceStore.js");
var import_firestoreRetriever = require("./firestoreRetriever.js");
const firebase = (0, import_core.genkitPlugin)(
  "firebase",
  (params) => __async(void 0, null, function* () {
    return {
      flowStateStore: {
        id: "firestore",
        value: new import_flow.FirestoreStateStore(params == null ? void 0 : params.flowStateStore)
      },
      traceStore: {
        id: "firestore",
        value: new import_firestoreTraceStore.FirestoreTraceStore(params == null ? void 0 : params.traceStore)
      }
    };
  })
);
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  defineFirestoreRetriever,
  firebase
});
//# sourceMappingURL=index.js.map